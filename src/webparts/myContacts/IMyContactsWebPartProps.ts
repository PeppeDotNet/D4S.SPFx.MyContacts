import { PersonaSize } from 'office-ui-fabric-react/lib/Persona';

export interface IMyContactsWebPartProps {
  pageSize: number;
  showPhone: boolean;
  pictureSize: PersonaSize;
  listId: string;
}
